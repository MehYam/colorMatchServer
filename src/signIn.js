const React = require('react');
const ReactDOM = require('react-dom');

class SignIn extends React.Component {
   render() {
      return (
         <div>This is the SignIn component</div>
      );
   }
}

module.exports = SignIn;